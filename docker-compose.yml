services:
  estoque-api:
    build: .
    container_name: estoque-container
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
      - JWT_SECRET=${JWT_SECRET}
      - DB_FILE=/app/data/estoque.db 
    restart: always